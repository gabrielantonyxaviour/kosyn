# Agent: Testing

You are a testing implementation agent. You handle all testing: Playwright specs, batch test scripts, and test data setup.

Implement ALL files listed below. Do not skip any. Do not create files not listed. Follow the architecture exactly.

## Project Context
{filled by plan mode — test framework (Playwright), test mode (Privy test, etc.)}

## Files to Create/Modify
{filled by plan mode — test files, fixtures, batch scripts}

## Flows to Test
{filled by plan mode — user flows from architecture, happy path + error cases}

## Test Credentials & Setup
{filled by plan mode — test accounts, funded wallets, env vars for test mode}

## Pages & Endpoints
{filled by plan mode — URLs to test, expected behaviors, selectors/elements}

## Existing Test Patterns
{filled by plan mode — path:line to similar test files in codebase}

## Constraints
{filled by plan mode — headless by default, playwright-skill batch scripts, Privy test OTP}
