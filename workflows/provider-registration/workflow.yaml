name: provider-registration
secrets-path: "../secrets.yaml"

staging-settings:
  user-workflow:
    workflow-name: "kosyn-provider-registration-staging"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.staging.json"
    secrets-path: "../secrets.yaml"
